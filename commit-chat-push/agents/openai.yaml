interface:
  display_name: "Commit Chat Push"
  short_description: "Commit code with its Codex transcript"
  default_prompt: "Use $commit-chat-push to commit these changes, add the Codex chat transcript, and push the branch."
